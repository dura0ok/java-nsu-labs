package fit.nsu.labs.storage;

import fit.nsu.labs.components.CarComponent;

public class RamCarComponentStorage extends RamStorage<CarComponent> {
    public RamCarComponentStorage(int capacity) {
        super(capacity);
    }
}
